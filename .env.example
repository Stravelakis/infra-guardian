# REAL runtime env. Paste real values locally. NEVER commit (see .gitignore, written at wrap-up).
# Placeholders below are intentional — Rule: tokens are never stored in-repo.

# infra-guardian — real env. GITIGNORED. Replace <...> locally. Never commit.
TELEGRAM_BOT_TOKEN=<paste-real-token-locally>
TELEGRAM_CHAT_ID=<paste-real-chat-id-locally>

# Model providers (only fill the ones you actually use)
MISTRAL_API_KEY=<...>
NVIDIA_API_KEY=<...>
GROQ_API_KEY=<...>
CEREBRAS_API_KEY=<...>
OPENROUTER_API_KEY=<...>
GEMINI_API_KEY=<...>
