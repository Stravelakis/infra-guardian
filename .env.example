# REAL runtime env. Paste real values locally. NEVER commit (see .gitignore, written at wrap-up).
# Placeholders below are intentional — Rule: tokens are never stored in-repo.

# --- Telegram (optional; matches EXAMPLE) ---
TELEGRAM_BOT_TOKEN=__PLACEHOLDER__
TELEGRAM_CHAT_ID=__PLACEHOLDER__

# --- Provider control-panel LOGIN URLs (non-secret notes only) ---
HOSTING_PANEL_URL=__PLACEHOLDER__
REGISTRAR_PANEL_URL=__PLACEHOLDER__
DNS_PANEL_URL=__PLACEHOLDER__
